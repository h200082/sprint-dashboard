/**
 * 역할: Next.js 호환 기능을 프로젝트 단위로 설정하는 중앙 구성 파일입니다.
 * 필요한 이유: 현재는 기본값을 사용하지만 향후 이미지, 리다이렉트 등의 프레임워크 옵션을 안전하게 추가할 위치가 필요합니다.
 */

import type { NextConfig } from 'next';

const nextConfig: NextConfig = {};

export default nextConfig;
